🧮 Python Calculator App
📌 Overview

This is a command-line calculator application built with Python. It performs basic arithmetic operations with strong input validation and error handling (including a custom exception for invalid operators). The app runs interactively until the user chooses to stop.

✨ Features

1. Basic arithmetic operations: +, -, *, /

2. Validates numeric input (handles non-numeric entries)

3. Validates math operators (handles invalid operators)

4. Prevents division by zero

5. Option to continue calculating or start afresh

🛠️ Technologies Used

Python 3 (standard library)

📂 Project Structure

calculator-app/
│
├── __init__.py
├── calc_art.py
├── calculator.py
└── README.md

File Descriptions

calculator.py — main calculator logic (user input, operations, loop control, error handling)

calc_art.py — calculator ASCII art / UI decorations (if used)

__init__.py — marks the directory as a Python package

👤 Author

Stephanie Chinenye Anowi